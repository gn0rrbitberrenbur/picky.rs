# picky.rs
picky is an application that lets you swipe through your images. Just select an folder containing .jpg/.raw filepairs and start swiping!

## Run
To run picky install Rust and Tauri.

Then clone the repo and run the following:
```
cd picky
npm run tauri dev
```
